apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fhir-extractor.fullname" . }}-test-connection"
  labels:
    {{- include "fhir-extractor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fhir-extractor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
